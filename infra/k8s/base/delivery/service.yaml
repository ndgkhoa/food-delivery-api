apiVersion: v1
kind: Service
metadata:
  name: delivery
  namespace: food-delivery
  labels:
    app.kubernetes.io/name: delivery
    app.kubernetes.io/part-of: food-delivery
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: delivery
  ports:
    - name: http
      port: 3005
      targetPort: http
