apiVersion: v1
kind: Service
metadata:
  name: review
  namespace: food-delivery
  labels:
    app.kubernetes.io/name: review
    app.kubernetes.io/part-of: food-delivery
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: review
  ports:
    - name: http
      port: 3009
      targetPort: http
