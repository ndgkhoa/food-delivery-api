apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: food-delivery
  labels:
    app.kubernetes.io/name: payment
    app.kubernetes.io/part-of: food-delivery
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: payment
  ports:
    - name: http
      port: 3007
      targetPort: http
