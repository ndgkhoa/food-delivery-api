apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: food-delivery
  labels:
    app.kubernetes.io/name: order
    app.kubernetes.io/part-of: food-delivery
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order
  ports:
    - name: http
      port: 3003
      targetPort: http
