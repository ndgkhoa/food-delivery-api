meta {
  name: Config - get value
  type: http
  seq: 2
}

get {
  url: {{gateway}}/config/:key
  body: none
  auth: bearer
}

params:path {
  key: order.delivery_fee_cents
}

auth:bearer {
  token: {{token}}
}
