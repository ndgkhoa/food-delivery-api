meta {
  name: Order - cancel
  type: http
  seq: 4
}

post {
  url: {{gateway}}/orders/{{orderId}}/cancel
  auth: bearer
}

auth:bearer { token: {{token}} }

docs { PENDING/RESERVED -> CANCELLED; releases the inventory hold. Owner (or admin). }
