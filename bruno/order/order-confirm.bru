meta {
  name: Order - confirm
  type: http
  seq: 3
}

post {
  url: {{gateway}}/orders/:orderId/confirm
  auth: bearer
}

params:path {
  orderId: 
}

auth:bearer {
  token: {{token}}
}

docs { RESERVED -> CONFIRMED. Owner (or admin). Optimistic-lock conflict -> 409. }
