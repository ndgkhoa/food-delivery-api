meta {
  name: Order - place
  type: http
  seq: 1
}

post {
  url: {{gateway}}/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Idempotency-Key: {{$guid}}
}

body:json {
  {
    "items": [
      { "itemId": "PASTE_A_MENU_ITEM_UUID", "qty": 2 }
    ]
  }
}
