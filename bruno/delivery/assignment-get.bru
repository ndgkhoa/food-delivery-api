meta {
  name: Delivery - order assignment
  type: http
  seq: 2
}

get {
  url: {{gateway}}/delivery/orders/:orderId/assignment
  auth: bearer
}

params:path {
  orderId: {{orderId}}
}

auth:bearer {
  token: {{token}}
}
