meta {
  name: Tenant - create
  type: http
  seq: 1
}

post {
  url: {{gateway}}/auth/tenants
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Acme Foods",
    "slug": "acme-foods"
  }
}
