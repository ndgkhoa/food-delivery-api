meta {
  name: Menu item - create
  type: http
  seq: 6
}

post {
  url: {{gateway}}/catalog/restaurants/:restaurantId/menu-items
  body: json
  auth: bearer
}

params:path {
  restaurantId: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Pho Bo",
    "description": "Beef noodle soup",
    "priceCents": 5000,
    "isAvailable": true
  }
}

docs {
  Owner/admin only. priceCents = integer cents. Copy the returned `id` into
  Order - place's body itemId (that item also needs an inventory stock row).
}
