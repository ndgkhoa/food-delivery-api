meta {
  name: Restaurant - list
  type: http
  seq: 2
}

get {
  url: {{gateway}}/catalog/restaurants
  auth: bearer
}

auth:bearer { token: {{token}} }

docs { Any authenticated tenant can read (tenant-scoped). }
