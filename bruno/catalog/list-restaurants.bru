meta {
  name: List restaurants
  type: http
  seq: 2
}

get {
  url: {{gateway}}/catalog/restaurants
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Any authenticated tenant can read. Reads are tenant-scoped to the token's
  tenant_id claim.
}
