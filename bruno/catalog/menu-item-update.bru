meta {
  name: Menu item - update
  type: http
  seq: 9
}

patch {
  url: {{gateway}}/catalog/restaurants/{{restaurantId}}/menu-items/{{menuItemId}}
  body: json
  auth: bearer
}

auth:bearer { token: {{token}} }

body:json {
  {
    "priceCents": 5500,
    "isAvailable": false
  }
}

docs { Owner/admin only. Partial update. }
